---
extends: default

rules:
  document-start:
    present: false

  line-length:
    max: 140
    level: warning

  empty-lines:
    max: 1

  truthy:
    allowed-values: ['true', 'false', 'on', 'off']
